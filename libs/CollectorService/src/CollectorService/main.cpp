
#include <iostream>

int main(int argc, char **argv)
{
    std::cout << "DataCollector Service: WIP" << std::endl;

    // Utilities - filter argv
    // Utilities - setup console signal handler (interrupt loop and safe cleanup)

    // Create DataCollector Instance
    // Register Collectors

    // Create DataPackager Instance
    // Setup to package data on new data collection

    // Create FileSystemMonitor
    // Link: onFileCreated - trigger to DataCollector

    // loop and wait for signal handler

    return 0;
}